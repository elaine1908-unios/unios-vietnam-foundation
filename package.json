{
  "name": "performance-profiles",
  "private": true,
  "version": "1.0.0",
  "engines": {
    "node": ">=22.5.0"
  },
  "scripts": {
    "build": "npm install --prefix web && npm run build --prefix web && npm install --prefix server && npm run build --prefix server",
    "start": "npm start --prefix server"
  }
}
